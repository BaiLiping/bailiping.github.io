/* Slide content, source anchors and presenter notes. Equations are LaTeX. */
(function(){
'use strict';
const r=String.raw;
const eq=(tex,small=false)=>`<div class="equation${small?' compact':''}">\\[${tex}\\]</div>`;
const card=(label,title,body,cls='')=>`<article class="card ${cls}"><div class="label">${label}</div><h3>${title}</h3>${body}</article>`;
const call=t=>`<div class="callout">${t}</div>`;
const P={K:'https://www.cs.cmu.edu/~kaess/pub/Kaess11icra.html',M:'https://www.cs.cmu.edu/~kaess/pub/Hsiao19icra.html',I:'https://www.cs.cmu.edu/~kaess/pub/Kaess08tro.html'};
const src=(p,t)=>`<a href="${P[p]}" target="_blank" rel="noopener">${t}</a>`;
const graph=`<svg class="diagram" viewBox="0 0 520 185" role="img" aria-label="A chain of poses with odometry, a loop closure and landmark observations"><g stroke="#8d9d92" stroke-width="2" fill="none"><path d="M50 100H460"/><path d="M50 100Q260 -25 460 100" stroke="#a94f2a"/><path d="M150 100L270 165L355 100" stroke="#4d6f8b"/></g><g fill="#203129"><rect x="96" y="95" width="10" height="10"/><rect x="201" y="95" width="10" height="10"/><rect x="301" y="95" width="10" height="10"/><rect x="406" y="95" width="10" height="10"/></g><g fill="#e8eee5" stroke="#2f6b4f" stroke-width="2"><circle cx="50" cy="100" r="23"/><circle cx="150" cy="100" r="23"/><circle cx="255" cy="100" r="23"/><circle cx="355" cy="100" r="23"/><circle cx="460" cy="100" r="23"/></g><g text-anchor="middle" font-size="16"><text x="50" y="106">x₀</text><text x="150" y="106">x₁</text><text x="255" y="106">x₂</text><text x="355" y="106">x₃</text><text x="460" y="106">x₄</text><text x="255" y="30" fill="#a94f2a">loop closure</text></g><circle cx="270" cy="165" r="13" fill="#e8eff3" stroke="#4d6f8b"/><text x="294" y="171" font-size="14">landmark</text></svg>`;
const hypo=`<svg class="diagram" viewBox="0 0 510 220" role="img" aria-label="Hypo-tree with one root, two first-layer hypotheses and four second-layer hypotheses"><g fill="none" stroke="#a6b2a5" stroke-width="2"><path d="M70 110L245 55L425 25M245 55L425 85M70 110L245 165L425 140M245 165L425 200"/></g><g fill="#e8eee5" stroke="#2f6b4f" stroke-width="2"><circle cx="70" cy="110" r="23"/><circle cx="245" cy="55" r="23"/><circle cx="245" cy="165" r="23"/></g><g fill="#f7e9df" stroke="#a94f2a" stroke-width="2"><circle cx="425" cy="25" r="20"/><circle cx="425" cy="85" r="20"/><circle cx="425" cy="140" r="20"/><circle cx="425" cy="200" r="20"/></g><g text-anchor="middle" font-size="14"><text x="70" y="115">∅</text><text x="245" y="60">+</text><text x="245" y="170">−</text><text x="425" y="30">++</text><text x="425" y="90">+−</text><text x="425" y="145">−+</text><text x="425" y="205">−−</text><text x="70" y="170">L₀</text><text x="160" y="24">L₁</text><text x="341" y="24">L₂</text></g></svg>`;
const slides=[
{id:'overview',section:'A reading map',title:'One Graph, Many Hypotheses',subtitle:'iSAM → iSAM2 → MH-iSAM2',layout:'cover-grid',body:`<div class="cover-copy"><div class="label">Incremental smoothing · discrete ambiguity</div><div class="cover-title">Reuse the work.<br><em>Keep the alternatives.</em></div><p>From a sparse least-squares system to a Bayes tree, then to several plausible maps.</p><p class="small">24 slides · 3 live experiments · equations and source-linked notes</p><svg viewBox="0 0 420 65" class="diagram" role="img" aria-label="One shared estimate branches into two hypotheses"><path d="M15 35H210L350 10M210 35L350 58" fill="none" stroke="#2f6b4f" stroke-width="2"/><g fill="#2f6b4f"><circle cx="15" cy="35" r="6"/><circle cx="100" cy="35" r="6"/><circle cx="210" cy="35" r="6"/></g><g fill="#a94f2a" class="pulse"><circle cx="350" cy="10" r="8"/><circle cx="350" cy="58" r="8"/></g></svg></div><div><a class="chapter-card" href="#problem"><div class="label">01 / the estimation problem</div><strong>iSAM</strong><span>Factor graphs, whitening and incremental QR.</span></a><a class="chapter-card" href="#bayes-tree"><div class="label">02 / reuse across time</div><strong>iSAM2</strong><span>Bayes-tree updates, ordering and relinearization.</span></a><a class="chapter-card" href="#ambiguity"><div class="label">03 / reuse across alternatives</div><strong>MH-iSAM2</strong><span>Multi-mode factors, Hypo-tree, inference and pruning.</span></a></div>`,source:src('K','[K11] iSAM2')+' · '+src('M','[M19] MH-iSAM2'),notes:'The deck follows the two supplied conference papers: Kaess et al., ICRA 2011, and Hsiao & Kaess, ICRA 2019. iSAM history is summarized in K11 and linked to the original 2008 paper. The experiments are original deterministic teaching models, not reproductions of paper datasets or production implementations.'},
{id:'problem',section:'01 / iSAM foundations',title:'The graph is the problem. iSAM is a solver.',subtitle:'Smoothing retains the trajectory, rather than estimating only the newest pose.',layout:'cols2',body:card('Model','What do we estimate?',graph+eq(r`\Theta=\{x_0,\ldots,x_T,\ell_1,\ldots,\ell_L\}`)+'<p>Poses, landmarks and any other modeled variables. A pose graph is the special case without explicit landmarks.</p>','sage')+card('Division of labor','What does the back-end receive?','<div class="flow"><div><b>Front-end</b>Measurements, candidate associations and initial values.</div><div><b>Factor graph</b>Variables, residual models and uncertainty.</div><div><b>Optimizer</b>A state estimate consistent with those constraints.</div></div>'+call('iSAM does not replace the SLAM formulation. It incrementally solves the sparse smoothing problem.')+'<p class="small">Odometry is one possible factor, not the definition of GraphSLAM. The measurement graph must supply enough information to estimate the states.</p>'),source:src('K','[K11] §I, §III, Fig. 2'),notes:'The source explicitly separates raw sensor processing, data association, and estimation. For fixed associations, the back-end optimizes the supplied graph. A prior or another gauge constraint is needed to remove unobservable global reference-frame freedom. This deck uses x for poses and ell for landmarks; the paper uses Theta for the full variable set.'},
{id:'objective',section:'01 / iSAM foundations',title:'A product of factors becomes a sum of squared errors.',subtitle:'For independent Gaussian measurement errors, use covariance-weighted residuals.',layout:'cols2',body:card('Probability','Factorization',eq(r`p(\Theta\mid Z)\propto\prod_k f_k(\Theta_k)`)+eq(r`f_k(\Theta_k)\propto\exp\!\left[-\tfrac12\|h_k(\Theta_k)-z_k\|_{\Sigma_k}^{2}\right]`,true)+'<p>Each factor touches only a small subset of variables. A prior can be included as another factor.</p>','sage')+card('Optimization','Sparse nonlinear least squares',eq(r`\hat\Theta=\arg\min_\Theta\tfrac12\sum_k\|e_k(\Theta_k)\|_{\Sigma_k}^{2}`)+eq(r`e_k=h_k(\Theta_k)-z_k,\quad\|e\|_\Sigma^2=e^\mathsf{T}\Sigma^{-1}e`,true)+call('Sparse dependence is what the solver exploits; Gaussian measurement noise does not make a nonlinear posterior globally Gaussian.')+'<p class="small">Teaching convention: include a prior for MAP, or fix the gauge and view the remaining objective as maximum likelihood.</p>'),source:src('K','[K11] §III, Eqs. (1)–(3)')+' · '+src('M','[M19] §III-A'),notes:'K11 formulates a general factorized objective and then Gaussian least squares. M19 states its ambiguity problem as MLE. The MAP notation here includes a prior as a factor; it is not a claim that M19 derives a Bayesian posterior over hypotheses. The nonlinear-versus-linearized Gaussian distinction is a mathematical clarification, not a quotation from M19.'},
{id:'linearize',section:'01 / iSAM foundations',title:'Linearize locally. Solve sparsely. Retract.',subtitle:'The same linear subproblem is underneath batch SAM, iSAM and iSAM2.',layout:'cols2',body:card('Local coordinates','Whiten the residual and its Jacobian',eq(r`e_k(\bar\Theta\oplus\delta)\approx e_k(\bar\Theta)+J_k\delta_k`)+eq(r`A_k=\Sigma_k^{-1/2}J_k,\qquad b_k=-\Sigma_k^{-1/2}e_k(\bar\Theta)`,true)+'<p>Stack the factor blocks to obtain the sparse matrix A and right-hand side b. The ⊕ operator applies a tangent-space increment.</p>','ice')+card('Linear algebra','Triangularize, then back-substitute',eq(r`\delta^*=\arg\min_\delta\|A\delta-b\|^2`)+eq(r`Q^\mathsf{T}[A\;b]=\begin{bmatrix}R&d\\0&r_\perp\end{bmatrix},\quad R\delta^*=d`,true)+eq(r`\hat\Theta=\bar\Theta\oplus\delta^*`)+'<p class="small">Cholesky is an alternative: factor AᵀA = RᵀR. The nonlinear problem generally needs repeated local solves.</p>'),source:src('K','[K11] §III, Eq. (4), Alg. 1; whitening written explicitly'),notes:'The sign convention is residual h-z, hence b is minus the whitened residual. Sigma^(-1/2) denotes a whitening matrix W with W^T W = Sigma^(-1). QR avoids explicitly forming normal equations. On a pose manifold, increments live in the tangent space. The browser demos below specialize this to Euclidean translations, so no manifold retraction is required.'},
{id:'isam',section:'01 / iSAM foundations',title:'iSAM: append new rows instead of starting over.',subtitle:'Keep the square-root information factor and restore triangular form with Givens rotations.',layout:'cols2',body:card('Reuse across measurements','Update the factorization',eq(r`\begin{bmatrix}R_t&d_t\\A_{\rm new}&b_{\rm new}\end{bmatrix}\xrightarrow{\text{Givens}}\begin{bmatrix}R_{t+1}&d_{t+1}\\0&r_\perp\end{bmatrix}`)+'<div class="flow"><div><b>Append</b>New variables add columns; new measurements add rows.</div><div><b>Rotate</b>Eliminate the nonzero entries below the diagonal.</div><div><b>Solve</b>Recover the updated trajectory by back-substitution.</div></div>','sage')+card('Why iSAM2 came next','Two operations were still periodic','<div class="large-number">QR</div><h4>Variable reordering</h4><p>Without a good ordering, fill-in accumulates and the factor becomes more expensive.</p><h4>Relinearization</h4><p>The original iSAM uses periodic batch steps to refresh nonlinear linearization points.</p>'+call('iSAM2 makes both operations incremental through the Bayes-tree representation.')),source:src('K','[K11] §II, §IV')+' · '+src('I','[I08] original iSAM'),notes:'New columns are implicitly zero-padded in old rows. The matrix display suppresses that bookkeeping. The original iSAM periodically reorders and relinearizes; iSAM2 removes the need for scheduled full batch steps, not the possibility that a particular update touches most of the graph.'},
{id:'qr-demo',section:'01 / live experiment',title:'Lab 1 · A loop closure changes old poses.',subtitle:'Add odometry, then close the loop. The incremental QR solution is checked against a fresh batch solve.',layout:'wide-left',lab:true,body:`<article class="card lab"><div class="controls"><button class="primary" id="qr-next">Add odometry</button><button id="qr-close">Close loop</button><button id="qr-reset">Reset</button><label>Drift scale <input id="qr-noise" type="range" min="0.08" max="0.50" step="0.02" value="0.24"><output id="qr-noise-value">0.24</output></label></div><svg id="qr-plot" class="plot" viewBox="0 0 620 300" role="img" aria-label="Ground truth, odometry and optimized translation trajectory"></svg><div class="legend"><span><i class="dot truth"></i>synthetic truth</span><span><i class="dot odom"></i>odometry only</span><span><i class="dot estimate"></i>optimized</span></div><div id="qr-status" class="status" aria-live="polite"></div></article>`+card('Actual computation','Givens updates, not an animation','<div class="stat-row"><div class="stat"><strong id="qr-poses">1</strong><span>poses, including fixed p₀</span></div><div class="stat"><strong id="qr-error">0</strong><span>sum of squared whitened residuals</span></div></div><div class="stat-row"><div class="stat"><strong id="qr-inc">0</strong><span>cumulative vector rows · incremental</span></div><div class="stat"><strong id="qr-batch">0</strong><span>cumulative vector rows · batch</span></div></div><p class="small">Batch versus incremental maximum coordinate difference:</p><div class="status" id="qr-difference">0</div>'+call('Teaching model: 2D translations, fixed p₀, isotropic factors. No rotations, fluid relinearization or Bayes-tree implementation here.')+'<p class="small">Rows count vector constraints, each with two scalar residuals. These are work counts, not measured runtime speedups. Changing drift resets the experiment.</p>','ice'),source:src('K','[K11] Eq. (4), §IV')+' · Original deterministic linear teaching model',notes:'The browser maintains R and two transformed right-hand sides, one for each coordinate. Adding a pose extends R; adding a factor applies Givens rotations to one whitened row. A separate factorization from all current measurements verifies the estimate. The synthetic odometry includes deterministic drift to make closure visible, so this is not a noise-consistency experiment. A zero residual on a tree does not imply an accurate trajectory.'},
{id:'bayes-tree',section:'02 / iSAM2',title:'A Bayes tree organizes the triangular system.',subtitle:'Each clique stores a conditional density for its frontal variables, given its separator variables.',layout:'cols2',body:card('One representation, two views','Conditionals ↔ blocks of R',eq(r`p(\delta\mid Z,\bar\Theta)=\prod_C p(\delta_{F_C}\mid\delta_{S_C})`)+eq(r`R_{FF}\delta_F+R_{FS}\delta_S=d_F`)+'<div class="flow"><div><b>F · frontals</b>Variables eliminated in this clique.</div><div><b>S · separator</b>Variables connecting the clique to its parent.</div></div><p class="small">At the root S is empty. These are conditionals of the current linearized Gaussian system.</p>','sage')+card('Interpretation','Not the same tree as the robot path','<svg class="diagram" viewBox="0 0 460 235" role="img" aria-label="Example Bayes tree with frontals and separators"><g stroke="#9aa99d" stroke-width="2"><path d="M230 52L118 121M230 52L345 121M118 150L118 204"/></g><g fill="#e8eee5" stroke="#2f6b4f"><rect x="150" y="15" width="160" height="52" rx="10"/><rect x="26" y="107" width="183" height="47" rx="10"/><rect x="255" y="107" width="180" height="47" rx="10"/><rect x="26" y="187" width="183" height="42" rx="10"/></g><g text-anchor="middle" font-size="17"><text x="230" y="47">x₃, x₄ | ∅</text><text x="118" y="137">x₁ | x₃</text><text x="345" y="137">x₂ | x₃, x₄</text><text x="118" y="214">x₀ | x₁</text></g></svg><p>Variable elimination and its ordering determine the clique structure. Sparse matrix updates become local tree-editing operations.</p>'+call('The separators explain which computations can be reused when another factor arrives.')),source:src('K','[K11] §IV, Fig. 3'),notes:'The diagram is an illustrative valid clique structure, not a reproduction of a particular dataset. A child separator is contained in its parent clique. A variable is frontal in exactly one clique. The original nonlinear posterior need not be Gaussian; the displayed density is the normalized linearized model with gauge fixed.'},
{id:'isam2-update',section:'02 / iSAM2',title:'Remove the affected top. Keep the orphan subtrees.',subtitle:'New factors and variables selected for relinearization contribute to the same update.',layout:'cols2',body:card('Upward: identify and rebuild','Follow affected cliques to the root','<div class="flow"><div><span class="step">01</span><b>Mark</b>Find touched variables and relevant cliques.</div><div><span class="step">02</span><b>Detach</b>Remove those cliques and their ancestors.</div></div><div class="flow"><div><span class="step">03</span><b>Cache</b>Preserve orphan subtrees and their marginal factors.</div><div><span class="step">04</span><b>Re-eliminate</b>Relinearize as required and reorder the affected variables.</div></div>','rose')+card('Downward: recover the estimate','Reattach, then solve','<div class="flow"><div><span class="step">05</span><b>Reattach</b>Insert the unchanged orphan subtrees into the new top.</div><div><span class="step">06</span><b>Back-substitute</b>Propagate updated separator values down the tree.</div></div>'+eq(r`\delta_F=R_{FF}^{-1}(d_F-R_{FS}\delta_S)`)+call('An unchanged conditional can still produce a changed estimate when its separator value changes.')+'<p class="small">Implementation uses triangular solves, not an explicit matrix inverse. “Reuse a subtree” does not mean “freeze its poses.”</p>'),source:src('K','[K11] Algs. 2–4, §IV-B–C'),notes:'The source caches marginal factors so elimination can restart in the middle of the tree. A numerical update can propagate below the re-eliminated top; the partial state update threshold determines when to stop. Do not interpret local refactorization as a guarantee that only local states move.'},
{id:'tree-demo',section:'02 / live experiment',title:'Lab 2 · Which part of the Bayes tree changes?',subtitle:'Compare elimination orders and factor endpoints. The highlight is computed from the resulting clique tree.',layout:'cols2',lab:true,body:`<article class="card lab"><div class="controls"><label>Ordering <select id="tree-order"><option value="chronological">Chronological / recent last</option><option value="balanced">Branched toy ordering</option><option value="recentFirst">Recent first</option></select></label><label>Update <select id="tree-event"><option value="append">New odometry (8 → 9)</option><option value="local">Short loop (6 ↔ 8)</option><option value="long">Long loop (0 ↔ 8)</option><option value="relin">Relinearize x₂</option></select></label></div><svg id="tree-plot" class="plot tree" viewBox="0 0 620 320" role="img" aria-label="Bayes tree with affected cliques and reusable subtrees"></svg><div class="legend"><span><i class="dot" style="background:#a94f2a"></i>rebuild</span><span><i class="dot" style="background:#2f6b4f"></i>reusable</span><span>F | S in each box</span></div><div class="lab-note">The pre-update tree is shown. For new odometry, x₉ is new and is not yet a clique in this tree.</div></article>`+`<article class="card lab ice"><div class="label">Symbolic elimination, computed in the browser</div><h3>Ordering changes the work.</h3><div class="stat-row"><div class="stat"><strong id="tree-affected">0</strong><span>existing cliques to rebuild</span></div><div class="stat"><strong id="tree-reused">0</strong><span>reusable cliques</span></div><div class="stat"><strong id="tree-fill">0</strong><span>fill-in edges</span></div></div><svg id="tree-matrix" class="plot" style="height:155px" viewBox="0 0 530 180" role="img" aria-label="Symbolic sparsity pattern of the triangular factor"></svg><div id="tree-status" class="status" aria-live="polite"></div><p class="lab-note">Try “Recent first” + new odometry. Then switch to “Chronological.” The touched pose moves from a leaf toward the root.</p><p class="lab-note">Exact symbolic fill-in and clique construction for this small graph. This is not a numerical iSAM2 update or an implementation of CCOLAMD.</p></article>`,source:src('K','[K11] Alg. 3, §IV-A')+' · Original symbolic example',notes:'There are nine existing scalar variable blocks and an anchored connected graph. The symbolic matrix displays block sparsity; it does not show actual numerical entries. For the relinearization case, mark every clique containing x2 as frontal or separator, plus its ancestors. In a production update, additional variables may be marked for relinearization. Work counts here cover the existing marked cliques only, not newly introduced cliques or subsequent back-substitution.'},
{id:'thresholds',section:'02 / iSAM2',title:'Two thresholds. Two different questions.',subtitle:'Relinearization changes the local model; partial back-substitution changes how far an update propagates.',layout:'cols2',body:card('β · fluid relinearization','Is the linearization point too old?',eq(r`\|\delta_j\|>\beta_j\quad\Longrightarrow\quad\bar\theta_j\leftarrow\bar\theta_j\oplus\delta_j`)+'<p>Compare the current estimate with its own linearization point. Recompute affected nonlinear factor linearizations when needed.</p>'+call('Test the magnitude of the accumulated increment, not only the latest small change.')+'<p class="small">A norm and per-variable scaling are teaching notation; different units should not be treated as interchangeable.</p>','sage')+card('α · partial state update','Will the next subtree change enough?',eq(r`\|\delta_j^{\rm new}-\delta_j^{\rm old}\|>\alpha_j`)+'<p>Continue downward when separator updates change enough. Otherwise retain the previous increments in the unreached subtree.</p>'+call('The α test concerns a change in δ, not the absolute size of δ.')+'<p class="small">Larger thresholds trade accuracy for work. They do not certify convergence or bound every pose error.</p>','cream'),source:src('K','[K11] §IV-B–C, Algs. 4–5'),notes:'This distinction is explicit in K11. The source uses alpha for partial back-substitution and beta for relinearization. It discusses accounting for different units. The norm and indexed thresholds here make the criterion readable; they are not claims about a particular current library API or its defaults. Coordinate bookkeeping clarification: after absorbing an increment into the linearization point, express the increment relative to the new point rather than applying the same increment twice.'},
{id:'isam2-sheet',section:'02 / equation sheet',title:'iSAM2 on one page',subtitle:'Same smoothing objective. A different schedule for doing the linear algebra.',layout:'cols2',body:card('Mathematical core','Model → linear system → clique solve',eq(r`\hat\Theta=\arg\min_\Theta\sum_k\|e_k(\Theta_k)\|_{\Sigma_k}^2`)+eq(r`\delta^*=\arg\min_\delta\|A\delta-b\|^2`)+eq(r`\delta_{F_C}=R_{FF,C}^{-1}(d_C-R_{FS,C}\delta_{S_C})`)+eq(r`\hat\Theta=\bar\Theta\oplus\delta^*`),'ice')+card('Update recipe','Reuse everything that remains valid','<div class="flow"><div><b>1 · Add</b>New factors and initialized variables.</div><div><b>2 · Mark</b>New-factor endpoints and stale linearization points.</div></div><div class="flow"><div><b>3 · Rebuild</b>Affected top, with cached orphan marginals and reordered variables.</div><div><b>4 · Solve</b>Reattach orphans; propagate increments as needed.</div></div>'+call('CCOLAMD constrains recently accessed variables toward the end of elimination—near the root—to reduce future update work.')+'<p class="small">Fill-in and future update locality are related but distinct ordering objectives.</p>'),source:src('K','[K11] Eq. (4), Algs. 2–5, §IV-A'),notes:'This is a compact synthesis of K11, not a new algorithm. Omitting the factor one half does not alter the minimizer. The inverse notation is shorthand for solving a triangular system. Keeping recent variables near the root may incur slightly more fill-in while reducing the affected region of subsequent updates.'},
{id:'ambiguity',section:'03 / MH-iSAM2',title:'A precise answer to the wrong graph is still wrong.',subtitle:'A single optimizer cannot preserve alternatives that the front-end has already discarded.',layout:'cols2',body:card('Continuous uncertainty','One locally Gaussian estimate',eq(r`\theta\approx\mathcal N(\mu,\Sigma)`)+'<svg class="diagram" viewBox="0 0 450 170" role="img" aria-label="A single local density peak"><path d="M15 140H435" stroke="#a2aea3"/><path d="M25 140C150 140 170 140 190 66C205 4 235 4 250 66C270 140 290 140 425 140" fill="none" stroke="#2f6b4f" stroke-width="4"/></svg><p>Covariance describes spread around an estimate. Increasing that spread does not explicitly encode two different associations.</p>','sage')+card('Discrete ambiguity','Several explanations of the measurements','<svg class="diagram" viewBox="0 0 450 170" role="img" aria-label="Two distinct candidate density peaks"><path d="M15 140H435" stroke="#a2aea3"/><path d="M20 140C60 140 72 135 90 63C106 4 132 4 148 63C167 140 175 140 215 140" fill="none" stroke="#2f6b4f" stroke-width="4"/><path d="M228 140C263 140 273 135 290 63C306 4 332 4 348 63C367 140 380 140 430 140" fill="none" stroke="#a94f2a" stroke-width="4"/></svg><p>Two odometry candidates. Two possible landmark associations. A loop closure that may be false.</p>'+call('MH-iSAM2 keeps multiple discrete choices long enough for later measurements to distinguish them.')),source:src('M','[M19] §I, §III-A–C'),notes:'The drawn curves are conceptual, not computed posterior densities. The paper distinguishes measurement modes (inputs) from state hypotheses (outputs). Its optimizer returns a collection of hypothesis-conditioned solutions, not a complete calibrated multimodal posterior density.'},
{id:'modes',section:'03 / ambiguity models',title:'Three multi-mode factors',subtitle:'An input mode changes a measurement, an association, or whether a factor exists.',layout:'cols3',body:card('Type #1','Multi-measurement',eq(r`z_r\in\{z_{r(1)},\ldots,z_{r(m)}\}`)+'<div class="flow"><div><b>Same variables</b>Different candidate measurements between them.</div></div><p>Two conflicting visual odometry estimates for the same pair of poses.</p><div class="matrix">xᵢ ⇉ xⱼ</div>','sage')+card('Type #2','Multi-association',eq(r`a_r\in\{\ell_1,\ldots,\ell_m\}`)+'<div class="flow"><div><b>Same observation</b>Different candidate variables to connect.</div></div><p>A detected feature could correspond to either of two landmarks.</p><div class="matrix">x → ℓ₁<br> ↘ ℓ₂</div>','ice')+card('Type #3','Boolean',eq(r`b_r\in\{0,1\}`)+'<div class="flow"><div><b>Factor on / off</b>The candidate constraint is present or absent.</div></div><p>A loop-closure proposal may be correct or an outlier.</p><div class="matrix">xᵢ ⋯ xⱼ</div><p class="small">Dropping a factor changes the residual dimension; raw error alone is not a fair pruning rule.</p>','rose'),source:src('M','[M19] §III-C, Fig. 2'),notes:'The source calls these multi-measurement, multi-association and Boolean factors. Modes are inputs; hypotheses are output combinations. M19 also discusses combining a Boolean alternative with other types to express that none of the candidates is valid. No claim here that continuous ambiguity is represented exactly.'},
{id:'mh-objective',section:'03 / the objective',title:'One mixture optimum ≠ a set of hypothesis optima.',subtitle:'MH-MLE explicitly conditions on a discrete choice for every ambiguous measurement.',layout:'cols2',body:card('Mixture model','Summing modes gives one objective',eq(r`P_M(z_r\mid\Theta_r)=\sum_{i=1}^{m_r}w_{r(i)}P(z_{r(i)}\mid\Theta_r)`,true)+eq(r`\hat\Theta=\arg\max_\Theta\prod_sP(z_s\mid\Theta_s)\prod_rP_M(z_r\mid\Theta_r)`,true)+'<p>Optimizing this product returns one maximizer. It does not automatically preserve the state estimate associated with every combination of input modes.</p>','cream')+card('Multi-hypothesis MLE','Solve conditional problems, keep alternatives',eq(r`\hat\Theta_{[h]}=\arg\max_\Theta\Big[\prod_sP(z_s\mid\Theta_s)\Big]\Big[\prod_r w_{r(h_r)}P(z_{r(h_r)}\mid\Theta_r)\Big]`,true)+eq(r`\hat\Theta_M=\{\hat\Theta_{[h]}:h\text{ retained}\}`)+'<p>Each h specifies one mode per multi-mode factor. The Hypo-tree and MH Bayes tree avoid independently duplicating all work.</p>'+call('These are hypothesis-conditioned optimized states, not posterior samples or normalized hypothesis probabilities.')),source:src('M','[M19] §III-A, Eqs. (2)–(5)'),notes:'The mathematical distinction follows M19. The paper writes all mode combinations before pruning; this slide writes retained h to reflect the practical algorithm. For a fixed h, positive weights that do not depend on Theta do not move its optimizer. Across hypotheses, weights and Gaussian normalization constants should not be confused with residual-only rankings. The demo uses equal mode weights and equal covariance/dimension across branches.'},
{id:'hypo-tree',section:'03 / hypothesis bookkeeping',title:'The Hypo-tree records the discrete history.',subtitle:'One new multi-mode factor adds a layer; each surviving node branches into its candidate modes.',layout:'cols2',body:card('Global hypotheses','A leaf is an entire choice sequence',hypo+eq(r`n_t=\prod_{r=1}^{t}m_r`)+'<p>Two binary factors produce four global hypotheses before pruning. A root-to-leaf path identifies the chosen mode of each factor.</p>','rose')+card('Local hypotheses','Not every variable needs all leaves',eq(r`\theta_p^{\{r\}}\longleftrightarrow L_r`)+'<p>Associate a variable or inference component with the Hypo-layer that indexes its current local alternatives.</p><div class="flow"><div><b>Ancestor lookup</b>Match a later hypothesis to its earlier prefix.</div><div><b>Descendant lookup</b>Find later alternatives that share an earlier value.</div></div>'+call('The Bayes tree organizes conditional dependence. The Hypo-tree organizes mode correspondence. They are different trees.')),source:src('M','[M19] §IV-A–C, Fig. 3'),notes:'The illustration is a simplified redraw of the growth mechanism in Fig. 3, not a copy of its pose graph. The root layer L0 contains the single initial hypothesis. Layer creation is chronological in multi-mode factor arrival. Pruning removes active branches, but later factor arrival does not require restructuring old Hypo-layers.'},
{id:'local-sharing',section:'03 / local hypotheses',title:'Share an early prefix—until later evidence reaches it.',subtitle:'A loop closure can make an old variable depend on a more recent ambiguity.',layout:'cols2',body:card('Before a closing constraint','Fewer local copies can be enough','<div class="branch-cards"><div><b>++</b><p class="small">global leaf</p></div><div><b>+−</b><p class="small">global leaf</p></div><div><b>−+</b><p class="small">global leaf</p></div><div><b>−−</b><p class="small">global leaf</p></div></div><div class="flow"><div><b>Shared “+” prefix</b>Two descendants may reuse the same earlier variable estimate or marginal.</div><div><b>Shared “−” prefix</b>The other pair can reuse another local value.</div></div>'+eq(r`\theta_p^{\{1\}}:\quad 2\text{ local hypotheses}`)+'<p class="small">Sharing follows the factor graph and inference dependencies; it is not simply “all old poses are shared.”</p>','sage')+card('After information propagates backward','Expand the variable’s layer association',eq(r`\theta_p^{\{1\}}\;\longrightarrow\;\theta_p^{\{2\}}`)+eq(r`2\text{ local hypotheses}\;\longrightarrow\;\text{up to }4`)+'<p>The newer mode can now change the best estimate of an earlier pose. Its local alternatives must expand accordingly.</p>'+call('Update the variable-to-layer association. The Hypo-tree itself does not need to be rebuilt.')+'<p class="small">Numerically similar compatible updates may later be merged. The paper notes that some redundant hypotheses can remain.</p>'),source:src('M','[M19] §IV-A–C, Figs. 3–4; §VII-D'),notes:'This is the key efficiency distinction from maintaining one full independent optimizer per global leaf. M19 Fig. 4 shows hypotheses expansion after adding a link to an earlier variable. Similar values are only merged respecting the Hypo-tree correspondence; coincident coordinates do not mean that whole joint hypotheses are identical.'},
{id:'mh-inference',section:'03 / multi-hypothesis Bayes tree',title:'Each clique eliminates compatible local hypotheses.',subtitle:'The Hypo-tree tells the MH Bayes tree which factor modes and messages belong together.',layout:'cols2',body:card('Collect and eliminate','Joint → conditional × marginal',eq(r`\phi_C^{[h]}(F,S)=\gamma_C^{[h]}(F\mid S)\,\omega_C^{[h]}(S)`)+'<div class="flow"><div><b>Collect</b>Single-/multi-mode factors and child marginal messages.</div><div><b>Match</b>Use ancestry to select compatible local hypotheses.</div><div><b>Eliminate</b>Factor out the frontals; pass a marginal upward.</div></div><p class="small">Paper terms: MHJD φ, MHCD γ and MHMD ω. A child marginal can be reused by multiple parent hypotheses.</p>','ice')+card('Block algebra · explanatory expansion','Partial Cholesky / Schur complement',eq(r`\Phi=\begin{bmatrix}H_{FF}&H_{FS}\\H_{SF}&H_{SS}\end{bmatrix},\quad g=\begin{bmatrix}g_F\\g_S\end{bmatrix}`)+eq(r`H_{\rm marg}=H_{SS}-H_{SF}H_{FF}^{-1}H_{FS}`,true)+eq(r`g_{\rm marg}=g_S-H_{SF}H_{FF}^{-1}g_F`,true)+'<p class="small">For quadratic energy ½δᵀHδ − gᵀδ, these blocks describe the outgoing Gaussian message. Keep the scalar constant as well when tracking residual error.</p>'+call('All hypotheses share one variable ordering. Ordering treats all association edges as connected and Boolean factors as “on.”')),source:src('M','[M19] §V-A–D; block equations expand the described partial factorization'),notes:'The source forms multi-hypothesis Hessians from augmented Jacobians, so its stored matrices include the right-hand side and scalar term. The Schur equations shown here are a pedagogical algebraic expansion, not extra equations quoted from the paper. They require a nonsingular eliminated block. In code, use triangular solves; do not form an inverse. The common ordering uses the union connectivity for multi-association/Boolean modes.'},
{id:'mh-backsub',section:'03 / returning to state estimates',title:'Back-substitution can expand local hypotheses.',subtitle:'A clique’s conditional and its separator increments may live at different Hypo-layers.',layout:'cols2',body:card('Match first, solve second','Correspondence matters at every step',eq(r`\delta_F^{[h]}=(R_{FF}^{[a_C(h)]})^{-1}\!\left(d_F^{[a_C(h)]}-R_{FS}^{[a_C(h)]}\delta_S^{[h]}\right)`,true)+'<p>The ancestor map a_C(h) selects the compatible local conditional through the Hypo-tree. A conditional can serve several descendant separator hypotheses.</p>'+call('Do not take a Cartesian product of unrelated local alternatives: that mixes incompatible histories.')+'<p class="small">Each ancestor map targets its component’s own Hypo-layer: C for the conditional, p for the state.</p>','sage')+card('Then retract','Merge compatible updates; expand when needed','<div class="flow"><div><b>1 · Solve</b>Compute increments using compatible conditionals and separator values.</div><div><b>2 · Merge</b>Try to combine numerically similar increments with valid correspondence.</div></div><div class="flow"><div><b>3 · Expand</b>Move the state variable to a later layer if more alternatives are required.</div><div><b>4 · Retract</b>Apply each increment to its corresponding linearization point.</div></div>'+eq(r`\theta_{p,[h]}\leftarrow\bar\theta_{p,[a_p(h)]}\oplus\delta_{p,[h]}`,true)),source:src('M','[M19] §IV-C, §V-E, Fig. 5(b)'),notes:'The source describes the maximum required hypothesis count among the conditional and separator increments, merging numerically similar delta values, then expanding a variable if needed. The ancestor maps a_C(h) and a_p(h) are teaching notation for correspondence to the conditional layer and state linearization-point layer, respectively; the two target layers need not coincide. M19 notes that local merging is not guaranteed to remove every numerical redundancy after nonlinear convergence.'},
{id:'pruning',section:'03 / controlling growth',title:'Pruning is part of the algorithm—not an afterthought.',subtitle:'The paper applies three criteria after elimination, then removes dead branches in both trees.',layout:'cols3',body:card('01 / statistical gate','Reject inconsistent fits',eq(r`e_h^2>\chi^2_{d_h,\,0.95}`)+eq(r`d_h=\dim(\text{factors})-\dim(\text{variables})`,true)+'<p>Prune hypotheses whose squared system error exceeds their 95% chi-square threshold.</p><p class="small">The implementation approximates nonlinear error using the linearized system’s residual.</p>','rose')+card('02 / desired count','Prefer more constrained fits',eq(r`n_{R1}>n_{\rm desire}`)+'<p>When too many survive, further prune hypotheses with fewer residual degrees of freedom.</p>'+call('Boolean “off” modes can reduce both residual and DoF. The smallest raw residual alone is not the paper’s rule.')+'<p class="small">This preference is relevant when branches contain different numbers of constraints.</p>','cream')+card('03 / hard limit','Bound the active set',eq(r`n_{R2}>n_{\rm limit}`)+'<p>Then remove lower chi-square-probability alternatives until the retained set satisfies the limit.</p>'+eq(r`n_{\rm limit}\ge n_{\rm desire}`)+'<p>Flag dead leaves, prune ancestors with no live descendants, and remove their associated values.</p><p class="small">A correct branch can be discarded. Later evidence cannot recover a deleted branch automatically.</p>','sage'),source:src('M','[M19] §VI-A–B, Fig. 6; risk discussed in §VII-D'),notes:'Residual chi-square interpretation assumes an appropriate Gaussian noise model, sufficient rank and the relevant local approximation. In the toy experiment, the fixed anchor removes gauge variables and only positive residual DoF are gated. Its branches all have the same DoF, making stage 2 inactive. It uses upper-tail chi-square consistency ordering, equivalent to increasing residual for fixed DoF. This is not a Bayesian posterior probability.'},
{id:'mh-demo',section:'03 / live experiment',title:'Lab 3 · Keep the right branch until evidence arrives.',subtitle:'Two ambiguous displacements create four candidate trajectories. Later constraints separate them.',layout:'wide-left',lab:true,body:`<article class="card lab"><div class="controls"><button id="mh-next" class="primary">Next event</button><button id="mh-reset">Reset</button><label>Keep at most <select id="mh-cap"><option value="4">4 hypotheses</option><option value="2">2 hypotheses</option><option value="1">1 hypothesis</option></select></label><label><input id="mh-gate" type="checkbox" checked>95% gate</label></div><div class="controls"><label>Later-measurement σ <input id="mh-sigma" type="range" min="0.14" max="0.80" step="0.02" value="0.22"><output id="mh-sigma-value">0.22</output></label><span class="lab-note">Changing settings resets the stream.</span></div><svg id="mh-plot" class="plot" viewBox="0 0 620 300" role="img" aria-label="Optimized y-position versus pose index for retained mode assignments"></svg><div id="mh-status" class="status" aria-live="polite"></div></article><article class="card lab ice"><div class="label">Hypo-tree and residual consistency</div><h3 id="mh-event">0 · Anchored start</h3><svg id="mh-tree" class="plot" style="height:142px" viewBox="0 0 530 180" role="img" aria-label="Hypothesis prefix tree; dashed nodes denote pruned branches"></svg><div class="table-wrap"><table class="tiny"><thead><tr><th>Modes</th><th>e²</th><th>DoF</th><th>Decision</th></tr></thead><tbody id="mh-table"></tbody></table></div><p class="lab-note" id="mh-detail"></p><p class="lab-note"><strong>Teaching model:</strong> exact batch linear least squares per candidate, plus prefix bookkeeping and irreversible pruning. No shared MH-Bayes-tree numerical factorization.</p><p class="lab-note">Try cap = 1 from the start. An arbitrary tie-break can delete the needed alternative before any evidence distinguishes it.</p></article>`,source:src('M','[M19] §III–VI')+' · Original linear branch-and-prune example',notes:'The synthetic correct branch is +−, selected by an endpoint constraint and then a mid-trajectory constraint. With cap 4 and default sigma, all four survive before evidence, the endpoint leaves +− and −+, and the middle observation leaves +−. With cap 1, lexicographic tie-breaking keeps ++, which the endpoint constraint rejects: the state becomes empty and is not silently rescued. With a large sigma or the gate disabled, ambiguity may remain. The plotted points are optimized per hypothesis; scores are not normalized posterior weights.'},
{id:'demo-model',section:'03 / experiment equation sheet',title:'What the multi-hypothesis lab actually computes',subtitle:'A small linear problem makes branching, delayed evidence and irreversible pruning inspectable.',layout:'cols2',body:card('Synthetic model','Two mode choices, then two measurements',eq(r`p_0=(0,0),\quad z_{01}=(1,0),\quad z_{23}=(1,0)`,true)+eq(r`z_{12}^{(a)}=(1,1.2a),\quad z_{34}^{(b)}=(1,1.2b)`,true)+eq(r`a,b\in\{+1,-1\}`)+'<div class="flow"><div><b>Later event 1</b>Measure p₄ − p₀ ≈ (4, 0).</div><div><b>Later event 2</b>Measure p₂ − p₀ ≈ (2, 1.2).</div></div>','cream')+card('Exact within this linear toy','Independent conditional solves',eq(r`\hat p^{[a,b]}=\arg\min_{p_1,\ldots,p_4}\sum_{(i,j)}\frac{\|p_j-p_i-z_{ij}^{[a,b]}\|^2}{\sigma_{ij}^2}`,true)+'<p>Anchor p₀ exactly. Initial displacement σ = 0.16. The later-measurement σ is controlled by the slider.</p>'+eq(r`d_h=2\,N_{\rm vector\ factors}-2\,N_{\rm free\ poses}`,true)+'<p>Equal mode weights, covariances and DoF across branches make residual ordering sufficient for this demonstration.</p>'+call('The lab demonstrates the problem and pruning logic. The shared local inference that makes MH-iSAM2 efficient is explained in the preceding slides.')),source:src('M','[M19] Eqs. (4)–(5), §VI')+' · Pedagogical model, not a paper experiment',notes:'All numbers here are original teaching choices, not experimental measurements from M19. Before redundant constraints arrive, residual DoF is zero and no chi-square gate is applied. The endpoint adds two residual DoF; the later midpoint adds two more. The 95% thresholds are 5.991464547 and 9.487729037. No hypothesis evidence integral or posterior normalization is computed.'},
{id:'comparison',section:'04 / synthesis',title:'What changes—and what stays the same?',subtitle:'GraphSLAM describes the estimation problem; these methods differ in how they maintain its solutions.',layout:'cols2',body:`<article class="card" style="grid-column:1/-1"><div class="table-wrap"><table><thead><tr><th>Question</th><th>iSAM</th><th>iSAM2</th><th>MH-iSAM2</th></tr></thead><tbody><tr><td>Continuous objective</td><td>Sparse nonlinear smoothing</td><td>The same class of problem</td><td>One conditional problem per retained mode combination</td></tr><tr><td>Main representation</td><td>Square-root information matrix</td><td>Bayes tree</td><td>MH Bayes tree + Hypo-tree</td></tr><tr><td>What is reused?</td><td>Incremental QR factors</td><td>Unaffected cliques and cached marginals</td><td>Temporal reuse + compatible local hypotheses</td></tr><tr><td>Relinearization / ordering</td><td>Periodic batch steps</td><td>Selective / incremental</td><td>Extended to multi-hypothesis inference; common ordering</td></tr><tr><td>Ambiguous input</td><td>A chosen graph / association</td><td>A chosen graph / association</td><td>Multi-measurement, multi-association and Boolean modes</td></tr><tr><td>Output</td><td>One optimized state</td><td>One optimized state</td><td>Several retained hypothesis-conditioned states</td></tr></tbody></table></div><div class="callout">Two kinds of reuse: <strong>iSAM2 reuses computations across time</strong>; <strong>MH-iSAM2 additionally shares computations across compatible hypotheses</strong>.</div></article>`,source:src('K','[K11] §II–IV')+' · '+src('M','[M19] §III–VI'),notes:'“One optimized state” means the usual fixed-graph use studied in these papers, not a claim that an external wrapper can never run multiple iSAM2 instances. MH-iSAM2 goes beyond such a wrapper by tracking local hypotheses and sharing inference. Current software versions or APIs are not compared here.'},
{id:'limits',section:'04 / limits that matter',title:'Incremental does not mean constant cost or certainty.',subtitle:'The graph, the active hypotheses and the nonlinear approximation all matter.',layout:'cols3',body:card('Computation','Local updates can become global','<p>iSAM2 is inexpensive when only a small top region changes. Long loop closures or extensive relinearization can touch much of the tree.</p>'+eq(r`n_t=\prod_r m_r`)+'<p class="small">MH-iSAM2 controls the active set by pruning. The unpruned combinatorial space still grows multiplicatively.</p>','cream')+card('Optimization','“Exact” needs a scope','<p>A linear least-squares solve can be exact up to numerical precision. A nonlinear per-hypothesis solve depends on initialization, iterations and linearization.</p>'+call('No guarantee here of finding every global nonlinear optimum.')+'<p class="small">The browser labs are linear by design; they do not establish nonlinear SLAM performance.</p>','ice')+card('Ambiguity','The right branch must survive','<p>A restrictive cap can remove the correct hypothesis before later evidence arrives. Wrong but statistically plausible constraints may also survive.</p>'+call('Discrete alternatives are the paper’s scope; a full continuous multimodal posterior is not its output.')+'<p class="small">The original paper explicitly identifies adaptive hypothesis limits and continuous ambiguity as directions for further study.</p>','rose'),source:src('K','[K11] §IV-D')+' · '+src('M','[M19] §VII-B–D')+' · Scope clarifications',notes:'K11 gives O(1) for a restricted exploration setting, and full-factorization bounds for more general updates; do not extrapolate universal constant time. M19 reports empirical speed with bounded hypotheses, not a theorem that ambiguity is free. M19 uses “exact solutions” to contrast hypothesis-conditioned optimization with sampling-based approximations; this deck does not interpret that as a global nonlinear optimality certificate.'},
{id:'references',section:'Sources & navigation',title:'Read the two papers alongside the experiments.',subtitle:'Slide footers identify the relevant sections. The teaching models are kept separate from the reported algorithms.',layout:'cols2',body:card('Primary sources','The supplied conference papers',`<div class="ref-item"><strong>[K11] Kaess et al. · ICRA 2011</strong><p><em>iSAM2: Incremental Smoothing and Mapping with Fluid Relinearization and Incremental Variable Reordering.</em> pp. 3281–3288.</p><p>§III: objective. §IV / Algs. 2–5: Bayes-tree update, ordering, state update and relinearization.</p>${src('K','Author publication page and PDF')}</div><div class="ref-item"><strong>[M19] Hsiao &amp; Kaess · ICRA 2019</strong><p><em>MH-iSAM2: Multi-hypothesis iSAM using Bayes Tree and Hypo-tree.</em> pp. 1274–1280.</p><p>§III: ambiguity models. §IV: Hypo-tree. §V: inference. §VI: pruning. §VII-D: limitations.</p>${src('M','Author publication page and PDF')}</div>`,'sage')+card('Background & experiments','Return to a mechanism',`<div class="ref-item"><strong>[I08] Kaess, Ranganathan &amp; Dellaert</strong><p><em>iSAM: Incremental Smoothing and Mapping.</em> IEEE Transactions on Robotics, 24(6), 1365–1378, 2008.</p>${src('I','Original iSAM publication')}</div><a class="chapter-card" href="#qr-demo"><strong>01 · Incremental QR</strong><span>Numerical updates and a batch-solve check.</span></a><a class="chapter-card" href="#tree-demo"><strong>02 · Bayes-tree locality</strong><span>Computed fill-in, cliques and affected ancestors.</span></a><a class="chapter-card" href="#mh-demo"><strong>03 · Delayed disambiguation</strong><span>Branch, add evidence, prune—or lose the answer.</span></a>`),source:'Original diagrams and deterministic teaching code · '+src('K','[K11]')+' · '+src('M','[M19]'),notes:'The deck uses the attached 2011 conference version of iSAM2, not the differently titled 2012 IJRR extension cited by the 2019 MH-iSAM2 paper. The original iSAM bibliographic entry is linked for background. No source PDFs are republished on this site. Print uses the current demo state; reset experiments before printing if desired.'}
];
window.ISAMSlides=slides;
})();
